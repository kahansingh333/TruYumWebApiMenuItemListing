apiVersion: v1
kind: Service
metadata:
    name: kahansinghtruyumwebapimenuitemlisting
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: kahansinghtruyumwebapimenuitemlisting